/**
 * Basic test file
 * This file ensures that jest tests can pass even if there are no actual tests yet
 */

describe('Basic test suite', () => {
  it('should pass', () => {
    expect(true).toBe(true);
  });
}); 